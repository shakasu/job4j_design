1. Создан объект User c полями и перекрытым методом finalize

2. Создано несколько объектов User и приблизительно они должны занимать памяти около 80 байт.
На практике это измерение не подтвердилось и даже не было постоянным, мне кажется, что
Runtime.getRuntime() обладает недостаточной точностью либо неприминим в данных условиях
3. 16 байт занимает пустой класс без полей.

4. при таких ключах -Xmx4m -Xms4m, примерно 120 итераций создания экземпляра заполняют всю память.

5. Метод main создает в цикле объекты без ссылок, эти "мусорные" объекты заполняю выделенные
для jvm 4мб (на самом деле около 2,5, потому что полтора выделяется на заранее сгенерированые
компоненты jvm).
При переполнении памяти происходит minor gc, major gc в данной программе невозможен, так как
нет объектов, которые могли бы "стареть" и увеличивать свой age.